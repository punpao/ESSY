{
  "name": "@thai-escrow/payment",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "nanoid": "^4.0.2"
  },
  "devDependencies": {
    "tsup": "^8.0.1"
  }
}
