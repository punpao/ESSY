{
  "name": "@thai-escrow/payment",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@thai-escrow/core": "workspace:*",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/qrcode": "^1.5.5",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
